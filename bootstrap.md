## Activar manualmente pestaña TAB

```js 
let tab = document.getElementById('idTab');

//activar pestaña detalle y contenido manualmente
const tabActivar = new bootstrap.Tab(tb);
tabActivar.show();
    
 ```
